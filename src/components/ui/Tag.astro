---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full bg-teal-dim px-3 py-1 font-mono text-xs text-teal"
>
  {label}
</span>
